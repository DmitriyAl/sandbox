apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kafka-producer-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kafka-producer-service
                port:
                  number: 8080
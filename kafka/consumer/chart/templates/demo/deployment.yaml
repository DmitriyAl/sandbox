apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.demo.image.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.demo.image.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.demo.image.name }}
    spec:
      containers:
        - name: {{ .Values.demo.image.name }}
          image: "{{ .Values.docker.repository }}/{{ .Values.demo.image.name }}:{{ .Values.demo.image.tag }}"
          ports:
            - containerPort: 8080
          env:
            - name: DB_HOST
              value: "demo-postgresql.default.svc.cluster.local"
            - name: DB_PORT
              value: "5432"
            - name: DB_USERNAME
              value: "postgres"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: demo-postgresql
                  key: postgres-password
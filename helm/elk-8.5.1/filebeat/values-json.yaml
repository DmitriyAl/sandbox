# values.yaml (только добавление inputs)
filebeatInputs:
  - type: log
    enabled: true
    paths:
      - /var/log/containers/*.log
    json.keys_under_root: true
    json.overwrite_keys: true
    json.add_error_key: true
    json.message_key: "message"